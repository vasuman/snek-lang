event Ping union:
  "A"
  "B"

module Example:
  state init:
    on entry:
      $event:e Ping."A"
      $pump local e
    on Ping."A":
      print 'got ping with A'
    on Ping:
      print 'parent trap'
  default init
